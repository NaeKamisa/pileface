#include <stdio.h>
#include <string.h>

int min(char* str){
    for(int i = 0;buf[i]!=0; i++){
        if(buf[i]>= 65 && buf[i] <= 90){
            buf[i] = buf[i] + 32;
        }
    }
    return 0;
}
